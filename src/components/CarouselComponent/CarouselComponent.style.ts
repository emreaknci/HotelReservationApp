
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    
  },
  carousel: {
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
});
